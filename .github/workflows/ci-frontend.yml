name: CI — Frontend (Next.js + TypeScript)

on:
  push:
    paths:
      - 'frontend/**'
      - '.github/workflows/ci-frontend.yml'
  pull_request:
    paths:
      - 'frontend/**'

jobs:
  build-test:
    name: Lint, Type-check, Test & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'frontend/apps/web/package-lock.json'

      - name: Install
        run: cd frontend/apps/web && npm ci

      - name: ESLint
        run: cd frontend/apps/web && npm run lint

      - name: TypeScript check
        run: cd frontend/apps/web && npm run typecheck

      - name: Prettier check
        run: cd frontend/apps/web && npm run format:check

      - name: Unit tests (Vitest)
        run: cd frontend/apps/web && npm test

      - name: Next.js build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:4000
          NEXT_PUBLIC_STRIPE_PK: pk_test_placeholder
        run: cd frontend/apps/web && npm run build

  e2e:
    name: Playwright E2E
    runs-on: ubuntu-latest
    needs: build-test
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install
        run: cd frontend/apps/web && npm ci
      - name: Install Playwright browsers
        run: cd frontend/apps/web && npx playwright install --with-deps chromium
      - name: Run Playwright tests
        run: cd frontend/apps/web && npx playwright test
        env:
          PLAYWRIGHT_BASE_URL: http://localhost:3000
